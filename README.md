# Customer Shopping Behavior Analysis

## Project Overview

This project focuses on analyzing customer shopping behavior to identify patterns, trends, and insights that can support better business decisions.

## Dataset

The dataset contains customer purchase data, including information related to buying patterns and behavior.

## Technologies Used

* Python
* Pandas
* PostgreSQL
* Jupyter Notebook
* Power BI

## Features

* Data preprocessing and cleaning
* Database integration using PostgreSQL
* Exploratory data analysis using Python
* Interactive data visualization using Power BI

## Environment Setup

Sensitive information such as database credentials is managed using environment variables.

Example `.env` file:

```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_username
DB_PASSWORD=your_password
```

## How to Run

1. Clone the repository
2. Create a `.env` file with your database credentials
3. Install required dependencies
4. Run the Jupyter Notebook for analysis
5. Open the Power BI file to view dashboards

## Future Improvements

* Advanced analytics and predictive modeling
* Integration with a web-based dashboard
